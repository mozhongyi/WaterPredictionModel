# 时间序列分析以及实验

## 实验场景一 ，ARCH模型和GARCH模型建模

        数据随时间的折线图，对其数据进行单位根检验，判断该数据是否为平稳性数据，若存在单位根，则称为平稳性数据，否则，被称为非平稳性数据。

        利用State做ADF检验，ADF检验的原假设：数据为单位根序列，备择假设：数据是平稳序列。平稳数据建模用ARMA模型，单位根数据建模用ARIMA模型。

        若检查不平稳，则进行差分处理后，再次进行ADF检验，直到ADF检验通过，序列平稳。

        序列平稳后，可以用ARMA进行建模。该模型需要确定阶数，可以用ACF图(自相关图)和PACF图(偏自相关图)进行分析确定。根据ACF图和PACF图，拟合四个模型进行选择，并通过AIC和BIC，AIC和BIC越小，模型越好。

        得到最好模型的估计结果后，得出模型的回归估计式子。

        此后，还需要检验€是否为白噪声，如果是白噪声，表明还未估计完全。画出残差序列的分布直方图，并使用Ljung-Box Q检验来检验ARMA模型的有效性。

        接着对残差的平方进行LM检验，以此来检验是否存在GARCH误差。若存在GARCH误差，则可以采用GARCH模型进行预测。
